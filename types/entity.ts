export interface UserToken {
    accessToken?: string;
    // refreshToken?: string;
}

export interface UserInfo {
  id: string;
  email: string;
  name: string;
  // còn thiếu trường gì viết vào đây
}